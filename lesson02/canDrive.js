const firstName = 'Mary';
const lastName = 'Sennello';
const age = 30;
let x = 16;

if (age >= x) {
  console.log (firstName + ' ' + lastName + ' ' + (age - x));
} else if (age < x) {
  for(let i = 5; i >= 0; i--)
    {console.log(i); }
}
